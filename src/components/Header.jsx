import styles from '@styles/Nav.module.scss';

const Header = () => (
  <nav className={styles.nav}>
    <ul>
      <li>
        <a href='#'>Sebastian Ermilin</a>
      </li>
    </ul>
  </nav>
);

export default Header;
